(new model m)
;=>

(add-rule m if '(?x ?y ?z) do (-> m '(?y ?z ?x)))
;=>

(add-stmt m '(a b c))
;=>

(start m)
;=>

(list-stmt m)
;=>

(make-ref-tree m '(c b a))
;=>

(defvar ?tree (make-ref-tree m '(c a b)))
;=>

?tree
;=>

(print-ref-tree ?tree)
;=>